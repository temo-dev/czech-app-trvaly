import 'package:flutter/material.dart';

class ExerciseExplanationScreen extends StatelessWidget {
  const ExerciseExplanationScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('ExerciseExplanationScreen')),
      body: const Center(child: Text('ExerciseExplanationScreen — TODO')),
    );
  }
}

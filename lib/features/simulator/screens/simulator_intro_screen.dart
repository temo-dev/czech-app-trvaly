import 'package:flutter/material.dart';

class SimulatorIntroScreen extends StatelessWidget {
  const SimulatorIntroScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('SimulatorIntroScreen')),
      body: const Center(child: Text('SimulatorIntroScreen — TODO')),
    );
  }
}
